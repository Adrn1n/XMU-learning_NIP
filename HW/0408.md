# 4-10
Routers drop the bad IP datagram because IP is connectionless and unreliable - no state is kept, and retransmission is the job of upper layers (like TCP).

The header checksum uses simple 16-bit one’s complement sum (not CRC) because it is much faster and easy to compute in software.

# 4-19
129.250.(16k)/20: 129.250.(16k).0, 129.250.(16k+15).255 (k=0~15)

# 4-20
3\. 1480b, 0, 1; 1480b, 185, 1; 1020b, 370, 0
